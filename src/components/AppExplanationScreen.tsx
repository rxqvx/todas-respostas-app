import React from "react";
import { View, Text, Button } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const AppExplanationScreen: React.FC = () => {
	return (
		<SafeAreaView>
			<Text>App Explanation Screen</Text>
		</SafeAreaView>
	);
};

export default AppExplanationScreen;
